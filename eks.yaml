apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig

metadata:
  name: expense
  region: us-east-1

managedNodeGroups:
  - name: expense
    instanceTypes: ["t3.medium", "t2.small","t2.micro","c5.large"]
    desiredCapacity: 3
    spot: true
